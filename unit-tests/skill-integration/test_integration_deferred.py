"""Section 9 integration tests — deferred (require Docker + live DB).

Tests 9.5–9.7 are integration tests that require a running PostgreSQL instance
and are deferred to the integration test suite.
Tests 9.1–9.4 are covered by their respective unit-test modules.
"""
