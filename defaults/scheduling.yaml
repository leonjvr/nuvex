scheduling:
  enabled: true
  global_limits:
    max_schedules_per_agent: 10
    max_schedules_per_division: 50
    max_total_scheduled_cost_per_day: 50.00
    min_cron_interval_minutes: 5
  deadline_watcher:
    enabled: true
    check_interval_ms: 60000
    warning_threshold_percent: 80
