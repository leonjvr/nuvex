{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal window permissions. No shell, filesystem, HTTP, dialog, clipboard, or notification plugin permissions are granted. All plugin access is deny-by-default in Tauri v2.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
