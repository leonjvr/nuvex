{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["auth_dir"],
  "properties": {
    "auth_dir": {
      "type": "string",
      "default": "./data/whatsapp-auth",
      "description": "Directory for WhatsApp auth state persistence. Must be writable and survive restarts."
    },
    "print_qr_terminal": {
      "type": "boolean",
      "default": true,
      "description": "Print QR code to terminal for first-time authentication."
    }
  }
}
