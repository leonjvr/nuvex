{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "port": {
      "type": "number",
      "default": 4201,
      "description": "WebSocket server port. Default 4201 sits next to the REST API on 4200."
    },
    "auth_mode": {
      "type": "string",
      "enum": ["token", "none"],
      "default": "token",
      "description": "Authentication mode. 'token' requires ?token=xxx query param on connect."
    },
    "auth_token": {
      "type": "string",
      "description": "Required when auth_mode=token. The token clients must supply in the URL query string."
    }
  }
}
