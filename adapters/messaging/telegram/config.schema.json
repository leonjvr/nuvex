{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["bot_token_secret"],
  "properties": {
    "bot_token_secret": {
      "type": "string",
      "description": "Secret name in SIDJUA secret store containing the Telegram Bot API token. Get yours from @BotFather on Telegram."
    },
    "allowed_chat_ids": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Restrict to specific chat IDs. Empty array = all chats (authorization via user mapping still applies)."
    },
    "drop_pending_updates": {
      "type": "boolean",
      "default": true,
      "description": "Skip messages received while the adapter was offline."
    }
  }
}
