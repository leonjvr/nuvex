{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["bot_token_secret", "app_token_secret"],
  "properties": {
    "bot_token_secret": {
      "type": "string",
      "description": "Secret name for Slack Bot Token (starts with xoxb-). Used for API calls."
    },
    "app_token_secret": {
      "type": "string",
      "description": "Secret name for Slack App Token (starts with xapp-). Required for Socket Mode connections."
    }
  }
}
