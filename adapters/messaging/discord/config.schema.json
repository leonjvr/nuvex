{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["bot_token_secret"],
  "properties": {
    "bot_token_secret": {
      "type": "string",
      "description": "Secret name for Discord Bot token. Create a bot via discord.com/developers/applications."
    },
    "guild_ids": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Restrict to specific guild (server) IDs. Empty array = all guilds the bot is in."
    }
  }
}
